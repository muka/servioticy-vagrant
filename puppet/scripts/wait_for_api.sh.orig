status=`/usr/bin/curl -X GET -s localhost:8080|grep 403| sed 's/ //g'`
while [ -z $status ]
do
	sleep 1
	status=`/usr/bin/curl -X GET -s localhost:8080|grep 403| sed 's/ //g'`
done
<<<<<<< HEAD
echo "API (Jetty) service running"
=======
echo "API (Jetty) service running"

>>>>>>> upstream/security
